# 电商平台开发文档

## 项目概述

### 1.1 项目简介
本项目是一个 B2C 电商平台，包含商品展示、购物车、订单管理、优惠券秒杀等功能模块。

### 1.2 技术栈

#### 后端技术栈
| 技术 | 版本 | 说明 |
|------|------|------|
| Java | 11 | 开发语言 |
| Spring Boot | 2.7.15 | 应用框架 |
| MyBatis-Plus | 3.5.3.2 | ORM 框架 |
| MySQL | 8.0+ | 数据库 |
| Lombok | 1.18.28 | 代码简化工具 |
| Maven | 3.6+ | 构建工具 |

#### 前端技术栈
| 技术 | 版本 | 说明 |
|------|------|------|
| Vue.js | 3.4.0 | 渐进式 JavaScript 框架 |
| Vue Router | 4.2.0 | 官方路由管理器 |
| Element Plus | 2.5.0 | UI 组件库 |
| Axios | 1.6.0 | HTTP 客户端 |
| Vite | 5.0.0 | 构建工具 |

### 1.3 项目结构

```
ecommerce/
├── backend/                          # 后端工程
│   ├── ecommerce-parent/            # 父工程（POM）
│   ├── ecommerce-common/            # 公共模块（工具类、常量等）
│   ├── ecommerce-pojo/              # 实体类、DTO、VO
│   └── ecommerce-core/              # 核心业务模块（Controller、Service、Mapper）
└── frontend/                         # 前端工程
    ├── src/
    │   ├── api/                     # API 接口
    │   ├── assets/                  # 静态资源
    │   ├── components/              # 公共组件
    │   ├── router/                  # 路由配置
    │   ├── utils/                   # 工具类
    │   ├── views/                   # 页面组件
    │   ├── App.vue                  # 根组件
    │   └── main.js                  # 入口文件
    └── package.json                 # 依赖配置
```

---

## 数据库设计

### 2.1 数据库环境
- **数据库类型**: MySQL 8.0+
- **字符集**: utf8mb4
- **排序规则**: utf8mb4_0900_ai_ci
- **存储引擎**: InnoDB

### 2.2 数据表设计

#### 2.2.1 分类表（category）

**表说明**: 商品分类和优惠券分类共用

```sql
CREATE TABLE `category` (
  `id` INT PRIMARY KEY AUTO_INCREMENT COMMENT '分类 ID',
  `name` VARCHAR(50) NOT NULL COMMENT '分类名称',
  `sort` INT NOT NULL DEFAULT 0 COMMENT '排序值',
  `status` TINYINT NOT NULL DEFAULT 1 COMMENT '状态：0-禁用，1-正常',
  `create_time` DATETIME DEFAULT CURRENT_TIMESTAMP COMMENT '创建时间',
  `update_time` DATETIME DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP COMMENT '更新时间',
  INDEX `idx_status` (`status`)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COMMENT='分类表';
```

**字段说明**:

| 字段名 | 类型 | 必填 | 默认值 | 说明 | 示例 |
|--------|------|------|--------|------|------|
| id | INT | 是 | 自增 | 分类 ID | 1 |
| name | VARCHAR(50) | 是 | - | 分类名称 | 食品生鲜 |
| sort | INT | 是 | 0 | 排序值（越小越前） | 1 |
| status | TINYINT | 是 | 1 | 状态：0-禁用，1-正常 | 1 |
| create_time | DATETIME | 否 | CURRENT_TIMESTAMP | 创建时间 | 2026-04-10 10:00:00 |
| update_time | DATETIME | 否 | CURRENT_TIMESTAMP | 更新时间 | 2026-04-10 10:00:00 |

**初始化数据**:

```sql
INSERT INTO `category` (`id`, `name`, `sort`, `status`) VALUES
(1, '食品生鲜', 1, 1),
(2, '数码电器', 2, 1),
(3, '户外运动', 3, 1),
(4, '美妆护肤', 4, 1);
```

**分类 ID 映射关系**:

| category_id | 分类名称 | 适用范围 |
|-------------|----------|----------|
| 1 | 食品生鲜 | 商品分类、优惠券分类 |
| 2 | 数码电器 | 商品分类、优惠券分类 |
| 3 | 户外运动 | 商品分类、优惠券分类 |
| 4 | 美妆护肤 | 商品分类、优惠券分类 |

---

#### 2.2.2 商品表（product）

```sql
CREATE TABLE `product` (
  `id` BIGINT PRIMARY KEY AUTO_INCREMENT COMMENT '商品 ID',
  `merchant_id` BIGINT NOT NULL DEFAULT 1 COMMENT '商家 ID（默认 1）',
  `name` VARCHAR(200) NOT NULL COMMENT '商品名称',
  `description` VARCHAR(1000) COMMENT '商品描述',
  `price` DECIMAL(10,2) NOT NULL COMMENT '价格',
  `stock` INT NOT NULL DEFAULT 0 COMMENT '库存',
  `sales` INT NOT NULL DEFAULT 0 COMMENT '销量',
  `category_id` INT NOT NULL COMMENT '分类 ID',
  `main_image` VARCHAR(500) COMMENT '主图 URL',
  `status` TINYINT NOT NULL DEFAULT 1 COMMENT '状态：0-下架，1-上架',
  `create_time` DATETIME DEFAULT CURRENT_TIMESTAMP COMMENT '创建时间',
  `update_time` DATETIME DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP COMMENT '更新时间',
  INDEX `idx_merchant_id` (`merchant_id`),
  INDEX `idx_category_id` (`category_id`),
  INDEX `idx_status` (`status`)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COMMENT='商品表';
```

**字段说明**:

| 字段名 | 类型 | 必填 | 默认值 | 说明 | 示例 |
|--------|------|------|--------|------|------|
| id | BIGINT | 是 | 自增 | 商品 ID | 1 |
| merchant_id | BIGINT | 是 | 1 | 商家 ID | 1 |
| name | VARCHAR(200) | 是 | - | 商品名称 | 新鲜草莓 500g |
| description | VARCHAR(1000) | 否 | - | 商品描述 | 当季新鲜草莓，现摘现发，香甜多汁 |
| price | DECIMAL(10,2) | 是 | - | 价格（元） | 29.90 |
| stock | INT | 是 | 0 | 库存数量 | 500 |
| sales | INT | 是 | 0 | 销量 | 1200 |
| category_id | INT | 是 | - | 分类 ID（关联 category 表） | 1 |
| main_image | VARCHAR(500) | 否 | - | 主图 URL | https://picsum.photos/400/400?random=1 |
| status | TINYINT | 是 | 1 | 状态：0-下架，1-上架 | 1 |
| create_time | DATETIME | 否 | CURRENT_TIMESTAMP | 创建时间 | 2026-04-10 10:00:00 |
| update_time | DATETIME | 否 | CURRENT_TIMESTAMP | 更新时间 | 2026-04-10 10:00:00 |

**初始化数据** - 食品生鲜类商品:

```sql
INSERT INTO `product` (`merchant_id`, `name`, `description`, `price`, `stock`, `sales`, `category_id`, `main_image`, `status`) VALUES
(1, '新鲜草莓 500g', '当季新鲜草莓，现摘现发，香甜多汁', 29.90, 500, 1200, 1, 'https://picsum.photos/400/400?random=1', 1),
(1, '有机苹果 5kg', '红富士苹果，脆甜可口，有机认证', 68.00, 800, 3500, 1, 'https://picsum.photos/400/400?random=2', 1),
(1, '进口车厘子 2kg', '智利进口车厘子，果大肉厚，新鲜直达', 198.00, 200, 890, 1, 'https://picsum.photos/400/400?random=3', 1),
(1, '新鲜蔬菜组合', '时令蔬菜组合，包含 6 种常见蔬菜', 39.90, 1000, 2100, 1, 'https://picsum.photos/400/400?random=4', 1),
(1, '海鲜大礼包', '包含大虾、扇贝、鱿鱼等海鲜', 299.00, 150, 560, 1, 'https://picsum.photos/400/400?random=5', 1);
```

**商品状态说明**:

| status | 状态 | 说明 |
|--------|------|------|
| 0 | 下架 | 商品已下架，用户不可见 |
| 1 | 上架 | 商品正常销售中 |

---

#### 2.2.3 优惠券表（coupon）

```sql
CREATE TABLE `coupon` (
  `id` INT(11) NOT NULL AUTO_INCREMENT COMMENT '优惠券 ID',
  `description` VARCHAR(500) COMMENT '优惠券简介',
  `category_id` INT(11) COMMENT '分类 ID，关联 category 表',
  `min_spend` DECIMAL(10,2) NOT NULL COMMENT '最低消费金额',
  `discount_amount` DECIMAL(10,2) NOT NULL COMMENT '可扣减金额',
  `start_time` DATETIME NOT NULL COMMENT '开始抢购时间',
  `end_time` DATETIME NOT NULL COMMENT '结束抢购时间',
  `valid_period` INT(11) NOT NULL COMMENT '有效期（天数）',
  `stock` INT(11) NOT NULL COMMENT '库存数量',
  `image` VARCHAR(255) COMMENT '优惠券图片路径',
  `status` TINYINT(1) DEFAULT 1 COMMENT '状态：1-启用，0-禁用',
  `create_time` DATETIME DEFAULT CURRENT_TIMESTAMP COMMENT '创建时间',
  `update_time` DATETIME DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP COMMENT '修改时间',
  PRIMARY KEY (`id`),
  KEY `idx_category_id` (`category_id`),
  KEY `idx_status` (`status`),
  KEY `idx_start_end_time` (`start_time`, `end_time`)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COMMENT='优惠券表';
```

**字段说明**:

| 字段名 | 类型 | 必填 | 默认值 | 说明 | 示例 |
|--------|------|------|--------|------|------|
| id | INT(11) | 是 | 自增 | 优惠券 ID | 1 |
| description | VARCHAR(500) | 否 | - | 优惠券简介 | 满 30 减 3，限食品生鲜类商品使用 |
| category_id | INT(11) | 否 | - | 分类 ID（关联 category 表） | 1 |
| min_spend | DECIMAL(10,2) | 是 | - | 最低消费金额（元） | 30.00 |
| discount_amount | DECIMAL(10,2) | 是 | - | 可扣减金额（元） | 3.00 |
| start_time | DATETIME | 是 | - | 开始抢购时间 | 2026-04-10 10:00:00 |
| end_time | DATETIME | 是 | - | 结束抢购时间 | 2026-04-10 23:59:59 |
| valid_period | INT(11) | 是 | - | 有效期天数（从领取日算起） | 15 |
| stock | INT(11) | 是 | - | 库存数量 | 1000 |
| image | VARCHAR(255) | 否 | - | 优惠券图片路径 | https://picsum.photos/300/150?random=1 |
| status | TINYINT(1) | 是 | 1 | 状态：1-启用，0-禁用 | 1 |
| create_time | DATETIME | 否 | CURRENT_TIMESTAMP | 创建时间 | 2026-04-10 10:00:00 |
| update_time | DATETIME | 否 | CURRENT_TIMESTAMP | 修改时间 | 2026-04-10 10:00:00 |

**优惠券状态说明**:

| status | 状态 | 说明 |
|--------|------|------|
| 0 | 禁用 | 优惠券已禁用，用户不可领取 |
| 1 | 启用 | 优惠券正常可领取 |

**分类 ID 对应关系**:

| category_id | 分类名称 | 适用商品范围 |
|-------------|----------|--------------|
| 1 | 食品生鲜 | 限食品生鲜类商品使用 |
| 2 | 数码电器 | 限数码电器类商品使用 |
| 3 | 户外运动 | 限户外运动类商品使用 |
| 4 | 美妆护肤 | 限美妆护肤类商品使用 |

**使用说明**:
1. 用户领取优惠券后，优惠券进入用户优惠券表
2. 优惠券有效期 = 领取时间 + valid_period（天数）
3. 订单金额 >= min_spend 时可使用该优惠券
4. 优惠券仅限指定分类（category_id）的商品使用

---

#### 2.2.4 用户表（user）

```sql
CREATE TABLE `user` (
  `id` INT(11) NOT NULL AUTO_INCREMENT COMMENT '用户 ID',
  `username` VARCHAR(50) NOT NULL COMMENT '用户名',
  `password` VARCHAR(100) NOT NULL COMMENT '密码（加密存储）',
  `nickname` VARCHAR(50) COMMENT '昵称',
  `phone` VARCHAR(20) COMMENT '手机号',
  `email` VARCHAR(100) COMMENT '邮箱',
  `avatar` VARCHAR(255) COMMENT '头像路径',
  `gender` TINYINT(1) DEFAULT 0 COMMENT '性别：0-未知，1-男，2-女',
  `status` TINYINT(1) DEFAULT 1 COMMENT '状态：1-正常，0-禁用',
  `create_time` DATETIME DEFAULT CURRENT_TIMESTAMP COMMENT '创建时间',
  `update_time` DATETIME DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP COMMENT '修改时间',
  PRIMARY KEY (`id`),
  UNIQUE KEY `idx_username` (`username`),
  UNIQUE KEY `idx_phone` (`phone`),
  UNIQUE KEY `idx_email` (`email`),
  KEY `idx_status` (`status`)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COMMENT='用户表';
```

**字段说明**:

| 字段名 | 类型 | 必填 | 默认值 | 说明 | 示例 |
|--------|------|------|--------|------|------|
| id | INT(11) | 是 | 自增 | 用户 ID | 1 |
| username | VARCHAR(50) | 是 | - | 用户名（唯一） | test1 |
| password | VARCHAR(100) | 是 | - | 密码（加密） | 123456 |
| nickname | VARCHAR(50) | 否 | - | 昵称 | 测试用户 1 |
| phone | VARCHAR(20) | 否 | - | 手机号（唯一） | 13800138001 |
| email | VARCHAR(100) | 否 | - | 邮箱（唯一） | test1@example.com |
| avatar | VARCHAR(255) | 否 | - | 头像路径 | https://picsum.photos/100/100?random=1 |
| gender | TINYINT(1) | 否 | 0 | 性别：0-未知，1-男，2-女 | 1 |
| status | TINYINT(1) | 是 | 1 | 状态：1-正常，0-禁用 | 1 |
| create_time | DATETIME | 否 | CURRENT_TIMESTAMP | 创建时间 | 2026-04-10 10:00:00 |
| update_time | DATETIME | 否 | CURRENT_TIMESTAMP | 修改时间 | 2026-04-10 10:00:00 |

**用户状态说明**:

| status | 状态 | 说明 |
|--------|------|------|
| 0 | 禁用 | 用户已被禁用，无法登录 |
| 1 | 正常 | 用户状态正常，可正常使用 |

**性别说明**:

| gender | 性别 | 说明 |
|--------|------|------|
| 0 | 未知 | 未设置性别 |
| 1 | 男 | 男性用户 |
| 2 | 女 | 女性用户 |

**初始化数据** - 测试用户:

```sql
INSERT INTO `user` (`username`, `password`, `nickname`, `phone`, `email`, `avatar`, `gender`, `status`) VALUES
-- 测试用户 1
('test1', '123456', '测试用户 1', '13800138001', 'test1@example.com', 'https://picsum.photos/100/100?random=1', 1, 1),
-- 测试用户 2
('test2', '123456', '测试用户 2', '13800138002', 'test2@example.com', 'https://picsum.photos/100/100?random=2', 2, 1),
-- 测试用户 3
('test3', '123456', '测试用户 3', '13800138003', 'test3@example.com', 'https://picsum.photos/100/100?random=3', 0, 1);
```

**测试账号说明**:

| 用户名 | 密码 | 昵称 | 手机号 | 性别 | 用途 |
|--------|------|------|--------|------|------|
| test1 | 123456 | 测试用户 1 | 13800138001 | 男 | 默认测试账号 |
| test2 | 123456 | 测试用户 2 | 13800138002 | 女 | 默认测试账号 |
| test3 | 123456 | 测试用户 3 | 13800138003 | 未知 | 默认测试账号 |

---

#### 2.2.5 地址表（address）

```sql
CREATE TABLE `address` (
  `id` BIGINT PRIMARY KEY AUTO_INCREMENT COMMENT '地址 ID',
  `user_id` BIGINT NOT NULL COMMENT '用户 ID',
  `name` VARCHAR(50) NOT NULL COMMENT '收货人姓名',
  `phone` VARCHAR(20) NOT NULL COMMENT '收货人电话',
  `province` VARCHAR(50) NOT NULL COMMENT '省份',
  `city` VARCHAR(50) NOT NULL COMMENT '城市',
  `district` VARCHAR(50) NOT NULL COMMENT '区/县',
  `detail_address` VARCHAR(255) NOT NULL COMMENT '详细地址',
  `is_default` TINYINT NOT NULL DEFAULT 0 COMMENT '是否默认：0-否，1-是',
  `create_time` DATETIME DEFAULT CURRENT_TIMESTAMP COMMENT '创建时间',
  `update_time` DATETIME DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP COMMENT '更新时间',
  INDEX `idx_user_id` (`user_id`)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COMMENT='地址表';
```

**字段说明**:

| 字段名 | 类型 | 必填 | 默认值 | 说明 | 示例 |
|--------|------|------|--------|------|------|
| id | BIGINT | 是 | 自增 | 地址 ID | 1 |
| user_id | BIGINT | 是 | - | 用户 ID | 1 |
| name | VARCHAR(50) | 是 | - | 收货人姓名 | 张三 |
| phone | VARCHAR(20) | 是 | - | 收货人电话 | 13800138000 |
| province | VARCHAR(50) | 是 | - | 省份 | 广东省 |
| city | VARCHAR(50) | 是 | - | 城市 | 深圳市 |
| district | VARCHAR(50) | 是 | - | 区/县 | 南山区 |
| detail_address | VARCHAR(255) | 是 | - | 详细地址 | XX 路 XX 号 |
| is_default | TINYINT | 是 | 0 | 是否默认地址 | 1 |

---

#### 2.2.6 购物车表（cart）

```sql
CREATE TABLE `cart` (
  `id` BIGINT PRIMARY KEY AUTO_INCREMENT COMMENT '购物车 ID',
  `user_id` BIGINT NOT NULL COMMENT '用户 ID',
  `product_id` BIGINT NOT NULL COMMENT '商品 ID',
  `product_name` VARCHAR(200) COMMENT '商品名称',
  `product_description` VARCHAR(500) COMMENT '商品描述',
  `product_image` VARCHAR(500) COMMENT '商品图片',
  `price` DECIMAL(10,2) NOT NULL COMMENT '单价',
  `quantity` INT NOT NULL DEFAULT 1 COMMENT '数量',
  `create_time` DATETIME DEFAULT CURRENT_TIMESTAMP COMMENT '创建时间',
  `update_time` DATETIME DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP COMMENT '更新时间',
  UNIQUE KEY `uk_user_product` (`user_id`, `product_id`),
  INDEX `idx_user_id` (`user_id`)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COMMENT='购物车表';
```

**字段说明**:

| 字段名 | 类型 | 必填 | 默认值 | 说明 | 示例 |
|--------|------|------|--------|------|------|
| id | BIGINT | 是 | 自增 | 购物车 ID | 1 |
| user_id | BIGINT | 是 | - | 用户 ID | 1 |
| product_id | BIGINT | 是 | - | 商品 ID | 1 |
| product_name | VARCHAR(200) | 否 | - | 商品名称 | 新鲜草莓 500g |
| product_description | VARCHAR(500) | 否 | - | 商品描述 | 当季新鲜草莓 |
| product_image | VARCHAR(500) | 否 | - | 商品图片 | https://xxx.com/img.jpg |
| price | DECIMAL(10,2) | 是 | - | 单价 | 29.90 |
| quantity | INT | 是 | 1 | 数量 | 2 |

---

#### 2.2.7 订单表（order）

```sql
CREATE TABLE `order` (
  `id` BIGINT PRIMARY KEY AUTO_INCREMENT COMMENT '订单 ID',
  `order_no` VARCHAR(50) NOT NULL COMMENT '订单号',
  `user_id` BIGINT NOT NULL COMMENT '用户 ID',
  `status` TINYINT NOT NULL DEFAULT 0 COMMENT '状态：0-待付款，1-待发货，2-待收货，3-已完成，4-已取消',
  `total_amount` DECIMAL(10,2) NOT NULL COMMENT '总金额',
  `freight_amount` DECIMAL(10,2) NOT NULL DEFAULT 0 COMMENT '运费',
  `pay_amount` DECIMAL(10,2) NOT NULL COMMENT '实付金额',
  `coupon_id` BIGINT COMMENT '使用的优惠券 ID',
  `coupon_amount` DECIMAL(10,2) COMMENT '优惠券金额',
  `payment_type` VARCHAR(20) COMMENT '支付方式',
  `payment_time` DATETIME COMMENT '支付时间',
  `receiver_name` VARCHAR(50) NOT NULL COMMENT '收货人',
  `receiver_phone` VARCHAR(20) NOT NULL COMMENT '收货电话',
  `receiver_province` VARCHAR(50) NOT NULL COMMENT '收货省份',
  `receiver_city` VARCHAR(50) NOT NULL COMMENT '收货城市',
  `receiver_district` VARCHAR(50) NOT NULL COMMENT '收货区/县',
  `receiver_detail_address` VARCHAR(255) NOT NULL COMMENT '收货详细地址',
  `remark` VARCHAR(500) COMMENT '订单备注',
  `create_time` DATETIME DEFAULT CURRENT_TIMESTAMP COMMENT '创建时间',
  `update_time` DATETIME DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP COMMENT '更新时间',
  UNIQUE KEY `uk_order_no` (`order_no`),
  INDEX `idx_user_id` (`user_id`),
  INDEX `idx_status` (`status`)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COMMENT='订单表';
```

**字段说明**:

| 字段名 | 类型 | 必填 | 默认值 | 说明 | 示例 |
|--------|------|------|--------|------|------|
| id | BIGINT | 是 | 自增 | 订单 ID | 1 |
| order_no | VARCHAR(50) | 是 | - | 订单号（唯一） | ORD202604090001 |
| user_id | BIGINT | 是 | - | 用户 ID | 1 |
| status | TINYINT | 是 | 0 | 订单状态 | 0 |
| total_amount | DECIMAL(10,2) | 是 | - | 总金额 | 3298.00 |
| freight_amount | DECIMAL(10,2) | 是 | 0 | 运费 | 0.00 |
| pay_amount | DECIMAL(10,2) | 是 | - | 实付金额 | 3298.00 |
| coupon_id | BIGINT | 否 | - | 优惠券 ID | 1 |
| coupon_amount | DECIMAL(10,2) | 否 | - | 优惠券金额 | 10.00 |
| payment_type | VARCHAR(20) | 否 | - | 支付方式 | 在线支付 |
| payment_time | DATETIME | 否 | - | 支付时间 | 2026-04-09 10:35:00 |
| receiver_name | VARCHAR(50) | 是 | - | 收货人 | 张三 |
| receiver_phone | VARCHAR(20) | 是 | - | 收货电话 | 13800138000 |
| receiver_province | VARCHAR(50) | 是 | - | 收货省份 | 广东省 |
| receiver_city | VARCHAR(50) | 是 | - | 收货城市 | 深圳市 |
| receiver_district | VARCHAR(50) | 是 | - | 收货区/县 | 南山区 |
| receiver_detail_address | VARCHAR(255) | 是 | - | 收货详细地址 | XX 路 XX 号 |
| remark | VARCHAR(500) | 否 | - | 订单备注 | 请尽快发货 |

**订单状态说明**:

| status | 状态 | 说明 |
|--------|------|------|
| 0 | 待付款 | 订单已创建，等待用户支付 |
| 1 | 待发货 | 用户已支付，等待商家发货 |
| 2 | 待收货 | 商家已发货，等待用户确认收货 |
| 3 | 已完成 | 用户已确认收货或自动确认 |
| 4 | 已取消 | 订单已取消（用户取消或超时取消） |

---

#### 2.2.8 订单商品表（order_item）

```sql
CREATE TABLE `order_item` (
  `id` BIGINT PRIMARY KEY AUTO_INCREMENT COMMENT '订单商品 ID',
  `order_id` BIGINT NOT NULL COMMENT '订单 ID',
  `product_id` BIGINT NOT NULL COMMENT '商品 ID',
  `product_name` VARCHAR(200) NOT NULL COMMENT '商品名称',
  `product_description` VARCHAR(500) COMMENT '商品描述',
  `product_image` VARCHAR(500) COMMENT '商品图片',
  `price` DECIMAL(10,2) NOT NULL COMMENT '单价',
  `quantity` INT NOT NULL COMMENT '数量',
  `total_price` DECIMAL(10,2) NOT NULL COMMENT '小计',
  `create_time` DATETIME DEFAULT CURRENT_TIMESTAMP COMMENT '创建时间',
  INDEX `idx_order_id` (`order_id`),
  INDEX `idx_product_id` (`product_id`)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COMMENT='订单商品表';
```

**字段说明**:

| 字段名 | 类型 | 必填 | 默认值 | 说明 | 示例 |
|--------|------|------|--------|------|------|
| id | BIGINT | 是 | 自增 | 订单商品 ID | 1 |
| order_id | BIGINT | 是 | - | 订单 ID | 1 |
| product_id | BIGINT | 是 | - | 商品 ID | 1 |
| product_name | VARCHAR(200) | 是 | - | 商品名称 | 新鲜草莓 500g |
| product_description | VARCHAR(500) | 否 | - | 商品描述 | 当季新鲜草莓 |
| product_image | VARCHAR(500) | 否 | - | 商品图片 | https://xxx.com/img.jpg |
| price | DECIMAL(10,2) | 是 | - | 单价 | 29.90 |
| quantity | INT | 是 | - | 数量 | 1 |
| total_price | DECIMAL(10,2) | 是 | - | 小计 | 29.90 |

---

#### 2.2.9 收藏表（favorite）

```sql
CREATE TABLE `favorite` (
  `id` BIGINT PRIMARY KEY AUTO_INCREMENT COMMENT '收藏 ID',
  `user_id` BIGINT NOT NULL COMMENT '用户 ID',
  `product_id` BIGINT NOT NULL COMMENT '商品 ID',
  `create_time` DATETIME DEFAULT CURRENT_TIMESTAMP COMMENT '收藏时间',
  UNIQUE KEY `uk_user_product` (`user_id`, `product_id`) COMMENT '用户对同一商品只能收藏一次',
  INDEX `idx_user_id` (`user_id`),
  INDEX `idx_product_id` (`product_id`)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COMMENT='收藏表';
```

**字段说明**:

| 字段名 | 类型 | 必填 | 默认值 | 说明 | 示例 |
|--------|------|------|--------|------|------|
| id | BIGINT | 是 | 自增 | 收藏 ID | 1 |
| user_id | BIGINT | 是 | - | 用户 ID | 1 |
| product_id | BIGINT | 是 | - | 商品 ID | 1 |
| create_time | DATETIME | 否 | CURRENT_TIMESTAMP | 收藏时间 | 2026-04-10 10:00:00 |

---

#### 2.2.10 用户优惠券表（user_coupon）

```sql
CREATE TABLE `user_coupon` (
  `id` BIGINT PRIMARY KEY AUTO_INCREMENT COMMENT '记录 ID',
  `user_id` BIGINT NOT NULL COMMENT '用户 ID',
  `coupon_id` BIGINT NOT NULL COMMENT '优惠券 ID',
  `status` TINYINT NOT NULL DEFAULT 0 COMMENT '状态：0-未使用，1-已使用，2-已过期',
  `order_id` BIGINT COMMENT '使用该优惠券的订单 ID',
  `get_time` DATETIME DEFAULT CURRENT_TIMESTAMP COMMENT '领取时间',
  `use_time` DATETIME COMMENT '使用时间',
  `end_time` DATETIME NOT NULL COMMENT '过期时间',
  UNIQUE KEY `uk_user_coupon` (`user_id`, `coupon_id`) COMMENT '每人限领一张',
  INDEX `idx_user_status` (`user_id`, `status`),
  INDEX `idx_end_time` (`end_time`)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COMMENT='用户领取优惠券表';
```

**字段说明**:

| 字段名 | 类型 | 必填 | 默认值 | 说明 | 示例 |
|--------|------|------|--------|------|------|
| id | BIGINT | 是 | 自增 | 记录 ID | 1 |
| user_id | BIGINT | 是 | - | 用户 ID | 1 |
| coupon_id | BIGINT | 是 | - | 优惠券 ID | 1 |
| status | TINYINT | 是 | 0 | 状态 | 0 |
| order_id | BIGINT | 否 | - | 订单 ID | 1 |
| get_time | DATETIME | 否 | CURRENT_TIMESTAMP | 领取时间 | 2026-04-10 10:30:00 |
| use_time | DATETIME | 否 | - | 使用时间 | 2026-04-10 14:20:00 |
| end_time | DATETIME | 是 | - | 过期时间 | 2026-04-25 23:59:59 |

**优惠券状态说明**:

| status | 状态 | 说明 |
|--------|------|------|
| 0 | 未使用 | 领取后未使用 |
| 1 | 已使用 | 已使用（订单支付成功） |
| 2 | 已过期 | 已过期（超过 end_time） |

---

## API 接口文档

### 3.1 接口规范

#### 3.1.1 请求格式
- **Content-Type**: `application/json`
- **字符编码**: `UTF-8`

#### 3.1.2 响应格式
```json
{
  "code": 1,        // 状态码：1-成功，0-失败，500-服务器错误
  "msg": "success", // 响应消息
  "data": {}        // 响应数据
}
```

#### 3.1.3 基础路径
- **后端接口**: `http://localhost:8080/`
- **前端服务**: `http://localhost:5174/`

---

### 3.2 用户模块

#### 3.2.1 用户登录

**接口地址**: `POST /user/login`

**请求参数**:
```json
{
  "username": "test1",
  "password": "123456"
}
```

**响应示例**:
```json
{
  "code": 1,
  "msg": "登录成功",
  "data": {
    "id": 1,
    "username": "test1",
    "nickname": "测试用户",
    "avatar": "https://xxx.com/avatar.jpg",
    "phone": "13800138000"
  }
}
```

**错误码**:
| code | msg | 说明 |
|------|-----|------|
| 0 | 用户名或密码错误 | 登录凭证错误 |
| 500 | 服务器内部错误 | 服务器异常 |

---

#### 3.2.2 用户注册

**接口地址**: `POST /user/register`

**请求参数**:
```json
{
  "username": "test1",
  "password": "123456",
  "phone": "13800138000"
}
```

**响应示例**:
```json
{
  "code": 1,
  "msg": "注册成功",
  "data": {
    "id": 1,
    "username": "test1",
    "nickname": "测试用户",
    "phone": "13800138000"
  }
}
```

**错误码**:
| code | msg | 说明 |
|------|-----|------|
| 0 | 用户名已存在 | 用户名重复 |
| 0 | 手机号已注册 | 手机号重复 |
| 500 | 服务器内部错误 | 服务器异常 |

---

### 3.3 商品模块

#### 3.3.1 获取商品列表

**接口地址**: `GET /product/list`

**请求参数**:
| 参数名 | 类型 | 必填 | 说明 |
|--------|------|------|------|
| categoryId | Integer | 否 | 分类 ID |
| pageNum | Integer | 否 | 页码（默认 1） |
| pageSize | Integer | 否 | 每页数量（默认 10） |

**响应示例**:
```json
{
  "code": 1,
  "msg": "success",
  "data": {
    "total": 100,
    "list": [
      {
        "id": 1,
        "name": "新鲜草莓 500g",
        "description": "当季新鲜草莓，现摘现发，香甜多汁",
        "price": 29.90,
        "sales": 1200,
        "category_id": 1,
        "main_image": "https://picsum.photos/400/400?random=1"
      }
    ]
  }
}
```

---

#### 3.3.2 获取商品详情

**接口地址**: `GET /product/detail`

**请求参数**:
| 参数名 | 类型 | 必填 | 说明 |
|--------|------|------|------|
| id | Long | 是 | 商品 ID |

**响应示例**:
```json
{
  "code": 1,
  "msg": "success",
  "data": {
    "id": 1,
    "merchant_id": 1,
    "name": "新鲜草莓 500g",
    "description": "当季新鲜草莓，现摘现发，香甜多汁",
    "price": 29.90,
    "stock": 500,
    "sales": 1200,
    "category_id": 1,
    "main_image": "https://picsum.photos/400/400?random=1",
    "status": 1
  }
}
```

---

### 3.4 分类模块

#### 3.4.1 获取分类列表

**接口地址**: `GET /category/list`

**请求参数**: 无

**响应示例**:
```json
{
  "code": 1,
  "msg": "success",
  "data": [
    {
      "id": 1,
      "name": "食品生鲜",
      "sort": 1,
      "status": 1
    },
    {
      "id": 2,
      "name": "数码电器",
      "sort": 2,
      "status": 1
    }
  ]
}
```

---

### 3.5 优惠券模块

#### 3.5.1 获取优惠券列表

**接口地址**: `GET /coupon/list`

**请求参数**:
| 参数名 | 类型 | 必填 | 说明 |
|--------|------|------|------|
| categoryId | Integer | 否 | 分类 ID |

**响应示例**:
```json
{
  "code": 1,
  "msg": "success",
  "data": [
    {
      "id": 1,
      "description": "满 30 减 3，限食品生鲜类商品使用",
      "category_id": 1,
      "min_spend": 30.00,
      "discount_amount": 3.00,
      "start_time": "2026-04-10 10:00:00",
      "end_time": "2026-04-10 23:59:59",
      "valid_period": 15,
      "stock": 1000,
      "image": "https://picsum.photos/300/150?random=1",
      "status": 1
    }
  ]
}
```

---

#### 3.5.2 领取优惠券

**接口地址**: `POST /coupon/grab`

**请求参数**:
```json
{
  "userId": 1,
  "couponId": 1
}
```

**响应示例**:
```json
{
  "code": 1,
  "msg": "领取成功",
  "data": null
}
```

**错误码**:
| code | msg | 说明 |
|------|-----|------|
| 0 | 优惠券已领完 | 库存不足 |
| 0 | 每人限领一张 | 重复领取 |
| 0 | 优惠券已过期 | 超过有效期 |

---

## 前端页面说明

### 4.1 页面结构

| 页面文件 | 路由 | 功能说明 | 关联数据表 |
|---------|------|----------|-----------|
| Home.vue | / | 首页，展示商品列表和分类 | product, category |
| ProductDetail.vue | /product/:id | 商品详情页 | product |
| CartSidebar.vue | 组件 | 购物车侧边栏 | cart |
| OrderDetail.vue | /order/detail | 订单确认页 | order, order_item |
| Personal.vue | /personal | 个人中心 | user, address, order, favorite |
| CouponSeckill.vue | /coupon | 优惠券秒杀页 | coupon, user_coupon |

---

### 4.2 核心业务流程

#### 4.2.1 用户登录/注册流程

```
用户打开应用 → 浏览商品（无需登录）
                ↓
        需要登录的操作（下单/抢券/收藏/加购物车）
                ↓
          弹出登录/注册对话框
                ↓
    ┌───────────┴───────────┐
    ↓                       ↓
密码登录              短信登录
(用户名 + 密码)      (手机号 + 验证码)
    ↓                       ↓
    └───────────┬───────────┘
                ↓
          登录成功
                ↓
    记录登录状态 (localStorage)
                ↓
          继续之前的操作
```

**注册流程**:
```
点击"立即注册" → 填写注册信息
                    ↓
        用户名 + 手机号 + 密码 + 确认密码
                    ↓
              表单验证
                    ↓
              调用注册接口
                    ↓
              注册成功
                    ↓
              自动切换到登录页
```

---

#### 4.2.2 优惠券秒杀流程

```
用户进入领券中心 → 浏览优惠券列表
                      ↓
              选择优惠券点击领取
                      ↓
              检查登录状态
                      ↓
          ┌───────────┴───────────┐
          ↓                       ↓
      未登录                   已登录
          ↓                       ↓
    弹出登录框            检查领取资格
                              ↓
                    ┌──────────────────┐
                    ↓                   ↓
                符合资格              不符合资格
                    ↓                   ↓
              调用领取接口          提示错误信息
                    ↓
              更新库存和状态
                    ↓
              显示领取成功
```

**领取资格检查**:
1. 优惠券库存 > 0
2. 用户在有效期内未领取过该优惠券
3. 优惠券在有效期内
4. 优惠券状态为启用

---

#### 4.2.3 订单创建流程

```
用户选择商品 → 加入购物车 → 选择商品提交订单
                                      ↓
                              选择收货地址
                                      ↓
                              选择可用优惠券
                                      ↓
                              确认订单信息
                                      ↓
                              创建订单
                                      ↓
                    ┌─────────────────┴─────────────────┐
                    ↓                                   ↓
                支付成功                            支付失败/取消
                    ↓                                   ↓
        更新订单状态为"待发货"                    更新订单状态为"已取消"
        更新优惠券状态为"已使用"                  释放优惠券（可重新使用）
        扣减商品库存
```

**优惠券使用规则**:
1. 订单金额 >= 优惠券最低消费金额（min_spend）
2. 优惠券分类与商品分类匹配
3. 优惠券在有效期内
4. 优惠券状态为未使用

---

## 部署说明

### 5.1 后端部署

#### 5.1.1 环境要求
- JDK 11+
- Maven 3.6+
- MySQL 8.0+

#### 5.1.2 启动命令
```bash
cd backend
mvn clean install
mvn spring-boot:run
```

---

### 5.2 前端部署

#### 5.2.1 环境要求
- Node.js 16+
- npm 8+

#### 5.2.2 安装依赖
```bash
cd frontend
npm install
```

#### 5.2.3 启动开发服务器
```bash
npm run dev
```

#### 5.2.4 生产构建
```bash
npm run build
```

---

## 开发规范

### 6.1 代码规范

#### 6.1.1 命名规范
- **类名**: 大驼峰命名（PascalCase），如 `UserController`
- **方法名**: 小驼峰命名（camelCase），如 `getUserInfo`
- **变量名**: 小驼峰命名，如 `userName`
- **常量名**: 全大写，下划线分隔，如 `DEFAULT_PAGE_SIZE`
- **数据库表名**: 小写，下划线分隔，如 `user_info`
- **数据库字段名**: 小写，下划线分隔，如 `user_name`

#### 6.1.2 注释规范
- 类必须有类注释，说明类的用途
- 公共方法必须有方法注释，说明方法功能、参数和返回值
- 复杂逻辑必须有行内注释

---


---


